📌 Task 1: Dynamic To-Do List
Objective:

Learn element creation, event handling, and DOM updates.

Features:

Input box + Add Task button

Add tasks dynamically to a <ul>

Each task contains:

Edit button

Delete button

Editable tasks update instantly without page refresh

📌 Task 2: Live Character Counter
Objective:

Practice real-time DOM updates.

Features:

Textarea with a character countdown (e.g., 100 characters left)

Updates count as the user types

Prevents typing once the limit is reached

Bonus:

Counter turns green → orange → red as the limit approaches

📌 Task 3: Image Carousel
Objective:

Learn timed DOM updates and event-driven interactions.

Features:

Slideshow of at least 5 images

Next and Previous navigation buttons

Auto-slide every 3 seconds using setInterval()

Bonus: Pause slideshow on mouse hover

📌 Task 4: Form Validation App
Objective:

Implement DOM-based validation and inline error messages.

Features:

Registration form with:

Name

Email

Password

Validation on form submit:

All fields required

Email must contain “@”

Password must be at least 8 characters

Inline error messages (no alert boxes)

Bonus:

Inputs show green border when valid

Red border when invalid

📌 Task 5: Interactive Product Filter
Objective:

Use array iteration + DOM manipulation for filtering.

Features:

List of at least 10 products categorized (e.g., electronics, clothing, books)

Three filter buttons:

All

Electronics

Clothing

Clicking filters the displayed products dynamically

🛠 Technologies Used

HTML5

CSS3

JavaScript (DOM Manipulation)

📂 Project Structure
/Frontend-Tasks
│── Task1-todo/
│── Task2-counter/
│── Task3-carousel/
│── Task4-form-validation/
│── Task5-product/
│── README.md
